# Week 2  Deep Learning (CNN)

**Goal:** Compare a simple CNN vs ResNet18 transfer learning on CIFAR-10.

**Plan**
1) Load CIFAR-10
2) Build small CNN from scratch
3) Fine-tune ResNet18 (pretrained)
4) Track accuracy/loss; save plots to plots/

**Env**
pip install torch torchvision torchaudio
